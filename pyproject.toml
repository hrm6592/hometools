[project]
name = "tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Masahisa Hori", email = "hrm.html@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
tools = "tools:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
